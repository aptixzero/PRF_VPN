# Generated by aggregate.py on every workflow run and published to the
# `bridge` release and the gh-pages branch — never to this branch.
#
# These were tracked once, and the result was a bot commit on main every 3
# hours in a repository whose whole point is to hold release artifacts. They
# are build output, so they are ignored here.
vless.txt
vmess.txt
status.json
